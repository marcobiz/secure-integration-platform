# Container packaging boundary

M0 riserva questa directory per il Gateway container. Il primo vertical slice usa esclusivamente un Gateway harness sotto `tests/e2e`; non viene pubblicata prematuramente un'immagine Gateway produttiva prima di M2.

