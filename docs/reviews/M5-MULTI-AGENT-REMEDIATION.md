# M5 multi-agent review remediation ledger

This ledger covers only the merge-blocking findings supplied for PR #5. LOW and NOTE observations remain outside this remediation gate. M5 is not declared Done by this document and PR #5 remains open and unmerged.

## Final merge-blocking cycle

Initial delta-review HEAD: `8d269509201bf320c98a65f6c7495c1b8cc16cce`. Implementation candidate exercised locally and by exact-commit CI before this documentation-only update: `9065d627ca3e70f9d4e56dd8a32d5aa0192735ff`. `main` remains `49f81cb37dcd5bf8956638fe4af53c3c5cf39b2b`; no history rewrite or merge was performed.

| Residual finding | Remediation and exact evidence | Status |
|---|---|---|
| SEC-01 four-eyes fail-closed | Production/OIDC startup rejects `RequireFourEyes=false`; only explicit loopback DevelopmentAuth can use the simplified policy. Publication, binding activation, self approval and stale approval are denied by application policy and PostgreSQL. | RESOLVED; CI PASS; read-only review pending |
| Binding DB immutability | Additive migration `0007_binding_bundle_immutability_m5.sql` uses trigger plus column-only `UPDATE(state)` grant. PG18 tests reject endpoint/checksum/scope/environment/revision tamper by non-superuser and preserve the published snapshot/provider boundary. | RESOLVED; CI PASS; read-only review pending |
| Remaining state/audit atomicity | Tenant/application create-update-disable and connector import/validate share their state transaction with audit. Fault injection, cancellation and middleware/application denial paths roll back or fail closed and emit one redacted event. | RESOLVED; CI PASS; read-only review pending |
| Exact approval UX | Canonical definition plus endpoint/secret/certificate binding revisions expose added/removed/changed redacted paths and component checksums. Approve submits the exact displayed publication digest; stale digest returns conflict. | RESOLVED; CI PASS; read-only review pending |
| Pagination/selectors | Total ordering has a unique tie-breaker; count is independent from empty pages; paged selectors reach records 51 and 101 with keyboard and accessible labels. | RESOLVED; CI PASS; read-only review pending |
| Dirty navigation, focus, i18n | Browser Back/Forward invokes the dirty guard, error summaries receive focus/live announcement, and remediation strings are present in EN/IT without key fallback. | RESOLVED; CI PASS; read-only review pending |
| Operational OpenAPI client | Runtime client operations derive routes/methods from OpenAPI `paths`; route/contract parity and generated-file drift checks fail closed; validation codes use the `BGW-*` convention. | RESOLVED; CI PASS; read-only review pending |
| Quickstart and full-stack invoke | `Workflow` creates version `2.0.0`, validates, binds the enrolled Installation Environment, requests/distinctly approves/publishes, grants, invokes through BGW1+mTLS, proves server-side API key/certificate, correlated audit, retire and deny. The synthetic provisioner itself now uses two-principal four-eyes. | RESOLVED; CI PASS; read-only review pending |
| PostgreSQL fail-closed tests | Missing connection configuration produces seven explicit SKIP results in the ordinary run; the dedicated PG18 gate executes eight real tests, including stores, SQL tamper, concurrency, sessions and pagination. General CI separates the superuser migration identity from the non-superuser admin-store test identity. | RESOLVED; CI PASS; read-only review pending |
| Canary/redaction | FULLSTACK-01 scans DOM, console/network capture, ProblemDetails/audit surfaces, container logs, screenshots/traces and result artefacts for per-run activation/session/binding/secret canaries. | RESOLVED; CI PASS; read-only review pending |
| Secret scanner control | The scanner now includes hidden and untracked files. A temporary synthetic `client_secret` fixture fails non-zero, is removed, and the clean tree passes. | RESOLVED; CI PASS; read-only review pending |
| Form contrast | Axe exposed MUI form labels/help text below 4.5:1 in the current Chromium. The theme now supplies explicit accessible colors without excluding the rule. | RESOLVED; `UI-MOCK-20` and 32/32 browser suite PASS |
| Concurrent binding publication | The exact-candidate CI exposed a legitimate alternate serializable winner and an unhandled PostgreSQL `40001`. Publication/mutation remains atomic in either ordering, and the store now translates serialization aborts to redacted `BGW-CONCURRENCY-CONFLICT`/HTTP 409. | RESOLVED; PG18 race repeated 10/10 PASS; exact-candidate CI PASS |

The local gate records 145 ordinary .NET PASS plus seven explicit PG SKIP, eight dedicated PostgreSQL 18 PASS, 22 Vitest PASS, 32 browser-mock PASS and one production-build full-stack PASS. Migration `0007` SHA-256 is `EC3B4E9A85FB050A1FDFC16E4E7B89042A7A6D93972CA46DF2BCEA2AE51FEE4A`. Exact-candidate CI is PASS: general run `31049726466` (6/6), M5 push run `31049725534` (15/15) and M5 PR run `31049726550` (15/15). This section does not declare M5 Done.

## Final technical remediation addendum — 2026-08-06

The four residual technical findings are closed on candidate `7ac956fe9d3fa641d420004c2f944c7a2f5a8210`: backend canonical semantic approval, transaction-local Tenant FORCE RLS, ETag/If-Match concurrency for Tenant/Application, and complete independent IT/EN resources. The updated gate records 149 ordinary .NET PASS, nine PostgreSQL 18 PASS, 25 Vitest PASS, 35 browser-mock PASS and FULLSTACK-01 PASS. Exact-candidate CI is green: general `31088473433` (6/6), M5 PR `31088472335` (15/15), M5 push `31088472535` (15/15). See `M5-TECHNICAL-FINAL-REMEDIATION.md` for the requirement-to-evidence delta. M5 remains not Done and PR #5 remains open/unmerged.

| Finding | Stato | Causa | Remediation applicata | Test regressivo / evidenza | Commit | Esito |
|---|---|---|---|---|---|---|
| A1 — FE-01 / PROD-01 | RESOLVED | The browser used a reduced hand-written schema and a non-canonical initial document. | The UI now consumes the repository-owned Draft 2020-12 schema and sample; AJV is preliminary and server validation remains authoritative. | `connectorSchema.test.ts`; Admin import API tests; `FULLSTACK-01` persists the canonical Draft in PostgreSQL. | `7f1126e` | PASS |
| A2 — SEC-01 | RESOLVED | Mutable endpoint and provider-reference dictionaries could be recombined after approval. | Immutable scoped binding bundle revisions are checksum-bound to four-eyes approval/publication; runtime resolves only the exact active approved revision. | Binding canary/scope/staleness/cache tests; PostgreSQL migration apply/no-op; `FULLSTACK-01`. | `b90271b` | PASS |
| A3 — SEC-02 | RESOLVED | Development authentication trusted HTTP host information and Compose was not explicitly loopback-bound. | Actual socket peer must be loopback; untrusted forwarding is ignored; development bind is `127.0.0.1`; Production remains fail-closed. | `AdminApiSecurityTests` loopback/remote/XFF/startup/Compose cases; full-stack runs Playwright in the Gateway network namespace. | `b90271b` | PASS |
| A4 — ARC-01 | RESOLVED | Approval checking and publication crossed transaction boundaries. | Approval lock, connector/binding digests, actor, row version, lifecycle transition and audit are one PostgreSQL transaction. | `M5_IT_DAT_Approved_binding_digest_and_publication_are_atomic_under_concurrent_mutation_when_configured`. | `b90271b` | PASS |
| B1 — ARC-02 / SEC-04 | RESOLVED | Administrative state and audit could commit independently. | Installation/HMAC, roles, approvals, publication, rollback, retire, grant and binding mutations are atomic at their PostgreSQL boundary. | `M5_IT_DAT_Fault_injection_rolls_back_admin_state_and_audit_when_configured`. | `ffcd39c`, `f432a4c` | PASS |
| B2 — ARC-03 / SEC-05 | RESOLVED | Relevant denials were not durably represented once and in redacted form. | Stable metadata-only denial audit is emitted once for RBAC, tenant, approval, CSRF, concurrency, bootstrap, role and binding denials. | `M5_IT_Security_denials_for_binding_policy_self_approval_and_bootstrap_are_redacted_once` and denial unit cases. | `ffcd39c`, `f432a4c` | PASS |
| B3 — ARC-04 | RESOLVED | Binding update accepted no precondition and changed a mutable row. | Create has no revision; update requires `If-Match`; stale/missing preconditions fail without mutation and successful updates create a revision. | `M5_IT_Binding_update_requires_current_IfMatch_and_precondition_failures_do_not_mutate`; `UI-MOCK-27`. | `ffcd39c` | PASS |
| B4 — ARC-05 / FE-02 | RESOLVED | Lists were truncated client-side and pagination was incomplete. | Stable bounded server pagination/filtering and accessible page controls cover every listed administration resource. | `AdminPaginationTests` with 101 records; API pagination tests; `UI-MOCK-28`. | `ffcd39c` | PASS |
| B5 — SEC-03 | RESOLVED | Authentication relied on a non-revocable browser ticket. | The cookie carries an opaque handle whose hash is stored server-side with idle/absolute expiry, rotation and revocation on logout/privilege change. | Session unit tests; PostgreSQL session test; `AdminOidcProtocolTests`; `FULLSTACK-01` logout replay denial. | `ffcd39c` | PASS |
| B6 — FE-03 | RESOLVED | Post-login 401 and backend outage were conflated. | 401 clears session/query/CSRF state and redirects; 503 remains retryable without stale authenticated data. | Session-context frontend tests and UI browser-mock cases. | `ffcd39c` | PASS |
| B7 — API-01 | RESOLVED | Route, schema and manual TypeScript contracts could drift. | Admin routes, bodies, responses, CSRF, ETag, pagination and problems are in OpenAPI; TypeScript types are generated and drift-checked. | OpenAPI route parity tests; `npm run check:api`; enum wire regression tests. | `ffcd39c`, `b879810`, `2dc0ff8` | PASS |
| C1 — FE-A11Y | RESOLVED | CodeMirror attributes and opened workflow surfaces were incompletely tested. | Attributes target the contenteditable node; labels, descriptions, summaries, focus and non-colour states are present. | `UI-MOCK-20`, `UI-MOCK-25`, `UI-MOCK-28`: zero critical/serious axe violations. | `f837244` | PASS |
| C2 — FE-07 | RESOLVED | Complex forms lacked shared dirty-navigation protection. | React Router navigation is guarded for connector, binding and grant editors with stay/discard choices and no sensitive local storage. | Dirty-state unit tests and `UI-MOCK-26`. | `f837244` | PASS |
| C3 — FE-09 | RESOLVED | Approval comparison was not a deterministic canonical JSON diff. | Added/removed/changed JSON paths and redacted old/new values are derived from canonical JSON and exposed without colour dependence. | Canonical diff unit tests; `UI-MOCK-23`; `UI-MOCK-28`. | `f837244` | PASS |
| C4 — PROD-02 | RESOLVED | The editor could replace a multi-binding set with one binding. | Endpoint, API-key and certificate logical maps are submitted atomically with revision preconditions; no values are retrieved. | Binding editor unit tests; `UI-MOCK-27`; `FULLSTACK-01`. | `f837244` | PASS |
| C5 — PROD-03 | RESOLVED | Quick start did not demonstrate installation enrollment. | Synthetic challenge, PoP and activation produce an Active installation displayed by the Admin plane. | M5 quick start and `FULLSTACK-01` Active-installation assertion. | `f837244` | PASS |
| C6 — PROD-05 | RESOLVED | The OSS export included excluded-document references and broken boundaries. | Allowlist and exported documentation are self-contained; export runs link, build, test, scan, licence and path checks. | `Export-OpenSourceCore.ps1` exact-tree validation. | `f837244` | PASS |
| D1 — TEST-01 | RESOLVED | `page.route` browser tests were incorrectly treated as product E2E and no real production stack was exercised. | The 28 UI mocks are explicitly isolated; a separate Playwright suite uses a production Gateway/Admin image, PostgreSQL 18 and real HTTP with distinct principals and no route interception. | `UI-MOCK-01..28`; `FULLSTACK-01`. | `503480c` | PASS |
| D2 — TEST-02 | RESOLVED | Real M5 stores were not proven under separated non-superuser roles; this exposed admin writes routed through runtime credentials and a missing role-revocation grant. | `IAdminGatewayRegistry` uses only the admin datasource; runtime remains least-privileged; additive migration `0006` grants DELETE only on role assignments. | PostgreSQL 18 `PostgresIsolationTests` 7/7 with migration/admin/runtime boundaries; full-stack grant mutation. | `f432a4c`, `503480c` | PASS |
| D3 — TEST-03/04/07 | RESOLVED | OIDC protocol, opened accessibility surfaces and cross-artifact canary scans were incomplete. | Real OIDC middleware tests cover protocol and negatives; axe covers operational surfaces; full-stack scans fresh browser/server artefacts and logs. | `AdminOidcProtocolTests` 2/2; `UI-MOCK-20/25/28`; `M5_FULLSTACK_REDACTION_PASS`. | `503480c` | PASS |
| D4 — TEST-05 | RESOLVED | Previous SBOM output did not inventory all distributed artefacts or assert known packages. | Per-artifact SPDX documents plus hashed aggregate cover Gateway, Broker, SDK, CLI, frontend and Gateway container. | `eng/validate-sbom.ps1` known-component and hash assertions. | `503480c` | PASS |
| D5 — Evidence | RESOLVED | The pre-remediation bundle cannot attest the corrected lineage. | A new redacted bundle is generated outside Git after the final documentation commit and binds its exact HEAD. | `C:\SecureEvidence\m5-final-remediation-gate-20260805-233357`; manifest/redaction/hash verification. | final documentation commit + external bundle | PASS |

## Scope and gate facts

- Initial reviewed HEAD: `15f2f0b9a931420250c3d01fc1a75d825f8800e3`.
- Effective remediation start HEAD: `a6a0461cddcf91cfc24eaa0bf4cbbc587e61059c`; the intervening diff is only `AGENTS.md`.
- M4 base: `49f81cb37dcd5bf8956638fe4af53c3c5cf39b2b`.
- Branch: `m5/admin-ui-mvp`; PR #5 remains open and unmerged.
- No confirmed merge-blocking finding was rejected. LOW/NOTE debt is intentionally deferred to normal backlog/release qualification.
- This ledger authorizes delta review only. It does not declare M5 Done and does not authorize M6.
