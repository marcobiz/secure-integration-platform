# M3A split-host — run bloccata 2026-08-04 13:17:18

## Classificazione

`m3a-live-20260804-131718` è **BLOCKED — PRE-HANDOFF INFRASTRUCTURE
VALIDATION** sul commit `2fa39f1179a5a625def6178563ab5df35df3dce8`.

Non è una run M3A PASS: il runner VM non è mai stato eseguito, P02 non è stato
provato e il relativo handoff non è riutilizzabile. Il record operativo preservato è
`C:\SecureEvidence\m3a-live-20260804-131718\PRE-HANDOFF-STATUS.json`, fuori Git.

## Evidenze aggiuntive e cause

- Gateway live: HTTP 200.
- Gateway readiness: HTTP 200.
- Docker health: FAIL. Il probe HTTPS interno usava `localhost`, assente dal SAN del
  certificato sintetico; la validazione TLS non era disabilitata.
- Windows Firewall: non enforcing. Domain, Private e Public erano tutti disabilitati,
  quindi la regola VM `172.29.109.23` → HOST `172.29.96.1:28443` non era una barriera
  dimostrabile.
- l'IPv4 HOST apparteneva a `vEthernet (Default Switch)`, InterfaceIndex `46`, ma
  `Get-NetConnectionProfile -InterfaceIndex 46` non restituiva un profilo. La rete
  fisica Ethernet era Public e Tailscale Private.

## Chiusura controllata

Il cleanup ufficiale è stato eseguito e verificato PASS:

- zero container, volumi e network della run;
- zero regole Windows Firewall temporanee e zero synthetic root certificate installati;
- zero file sensibili temporanei, PFX o bootstrap/activation code residui;
- handoff non usato verificato tramite RunId e rimosso dalla VM;
- repository VM pulito e invariato; zero task M3 residui;
- `PRE-HANDOFF-STATUS.json` preservato, SHA-256
  `19A6FFE160C49EEF059672D50992670BBFDC0F4EB50B53DA0B86968C73EE0886`.

I record redatti di classificazione e cleanup rimangono nella directory evidence
esterna. Nessun dato raw o evidence è stato aggiunto al repository.

## Correzioni e blocker successivo

Il certificato Gateway include ora SAN `localhost`, `127.0.0.1`, nome logico e IP
HOST; la CI verifica health, live, readiness, validazione TLS e il fallimento con SAN
incompatibile, vietando bypass permissivi.

Il runner firewall ora salva lo stato originale dei profili, installa un rollback
SYSTEM a scadenza, abilita soltanto il profilo risolto, limita la regola a interfaccia e
tuple VM/HOST/porta e verifica l'`ActiveStore`; cleanup e fail-safe ripristinano lo
stato esatto.

La topologia Default Switch corrente resta un blocker: non esiste un profilo associato
all'InterfaceIndex 46. Serve una rete Hyper-V interna dedicata con profilo verificabile
e senza impatto sulle connessioni fisiche/necessarie prima di generare un nuovo RunId,
activation code, certificati o handoff.
