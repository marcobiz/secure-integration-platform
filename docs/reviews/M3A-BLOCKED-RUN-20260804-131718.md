# M3A split-host — Blocked run 2026-08-04 13:17:18

## Classification

`m3a-live-20260804-131718` is **BLOCKED — PRE-HANDOFF INFRASTRUCTURE
VALIDATION** on commit `2fa39f1179a5a625def6178563ab5df35df3dce8`.

This is not an M3A PASS run: the VM runner was never executed, P02 was not
tested and its handoff cannot be reused. The preserved operational record is
`C:\SecureEvidence\m3a-live-20260804-131718\PRE-HANDOFF-STATUS.json`, outside Git.

## Additional evidence and causes

- Gateway live: HTTP 200.
- Gateway readiness: HTTP 200.
- Docker health: FAIL. The internal HTTPS probe used `localhost`, absent from the synthetic
  certificate SAN; TLS validation was not disabled.
- Windows Firewall: not enforcing. Domain, Private and Public were all disabled,
  so the rule VM `172.29.109.23` → HOST `172.29.96.1:28443` was not a demonstrable
  barrier.
- the HOST IPv4 belonged to `vEthernet (Default Switch)`, InterfaceIndex `46`, but
  `Get-NetConnectionProfile -InterfaceIndex 46` returned no profile. The physical
  Ethernet network was Public and Tailscale Private.

## Controlled closure

Official cleanup was executed and verified PASS:

- zero run containers, volumes and networks;
- zero temporary Windows Firewall rules and zero installed synthetic root certificates;
- zero residual temporary sensitive files, PFX or bootstrap/activation codes;
- unused handoff verified by RunId and removed from the VM;
- VM repository clean and unchanged; zero residual M3 tasks;
- `PRE-HANDOFF-STATUS.json` preserved, SHA-256
  `19A6FFE160C49EEF059672D50992670BBFDC0F4EB50B53DA0B86968C73EE0886`.

Redacted classification and cleanup records remain in the external evidence
directory. No raw data or evidence was added to the repository.

## Corrections and subsequent blocker

The Gateway certificate now includes SANs for `localhost`, `127.0.0.1`, logical name and
HOST IP; CI verifies health, live, readiness, TLS validation and failure with an incompatible
SAN, prohibiting permissive bypasses.

The firewall runner now saves original profile states, installs an expiring SYSTEM
rollback, enables only the resolved profile, restricts the rule to the interface and
VM/HOST/port tuple and verifies `ActiveStore`; cleanup and fail-safe restore the
exact state.

The runner no longer accepts the Default Switch topology. Subsequent support
creates an `M3A-Isolated` Hyper-V Internal segment, retains the management NIC, uses
static addresses without NAT/gateway/DNS/forwarding, temporarily isolates the Private
profile by disabling Tailscale and installs a SYSTEM rollback. The correction does not
retroactively change this run's classification: a new execution with new
RunId, activation codes, certificates and handoff must demonstrate it live.
