# M5.5 Direct Gateway Access — implementation report

Date: 2026-08-07
Baseline: `8774c252b233456173c3ab31346fb21390fb8d7d`
Product candidate: `1b3a3b38fa7d01c8c5f96af0324d040e412ac0be`
Validated test-harness HEAD: `9a518eb`
Branch: `m55/direct-gateway-access`

## Local result

The Direct path is implemented without a second runtime. Broker and Direct produce the
same `GatewayClientPrincipal` and use shared grants, Published catalog, bindings,
providers, restricted egress and audit. M5 remains frozen; M6, healthcare connectors,
Keycloak, Redis, legacy adapters and Hyper-V have not started.

## Design and compatibility

- BGW1 and enrollment/runtime routes remain unchanged;
- `ActivationRequest.BrokerVersion` remains compatible; Direct uses `ClientVersion`;
- `/v1/broker-policy` remains Broker-only;
- migration 0011 adds type/version/update timestamp and migrates all M5 rows to Broker;
- the M2 function `resolve_installation_identity` retains its signature and return type; the new `resolve_installation_client_metadata` is additive and least-privilege;
- no endpoint/provider/locator or outbound credential appears in the direct request.

## Named evidence

| Area | Evidence | Result |
|---|---|---|
| Broker + Direct share runtime | `M55_UT_Broker_and_Direct_principals_converge_on_the_same_runtime_pipeline` | PASS |
| Enrollment/PoP/replay | `UT_GTW_Enrollment_PoP_derives_tenant_and_replay_is_rejected` | PASS |
| Direct signature/mTLS/timestamp/replay/revocation/grant | `M55_UT_Direct_installation_rejects_Broker_version_field_signature_replay_revocation_and_missing_grant` | PASS |
| Closed request authority | `UT_GTW_Invoke_contract_has_no_client_controlled_endpoint_or_secret_reference` | PASS |
| Deny before side effects | `UT_EGR_Ungranted_operation_is_denied_before_DNS_vault_or_transport` | PASS |
| Anti-exfiltration | `M5_UT_Instrumented_actual_canary_provider_is_called_only_for_published_approved_destination` | PASS |
| PostgreSQL Direct and Broker | `IT_DAT_PostgreSQL18_registry_enrollment_grant_replay_and_revocation_when_configured` | PASS PostgreSQL 18 |
| RLS/least privilege | 10 `PostgresIsolationTests` tests with non-superuser roles | 10/10 PASS |
| Admin API | `M5_IT_Installation_activation_is_returned_once_and_never_listed` | PASS |
| Admin UI | `M55-UI-MOCK Direct installation selection is authoritative and public metadata only` | PASS |

## Migration

File: `0011_direct_installation_m55.sql`
SHA-256: `62D79829F1B3DF072E563A415B9380053227C6BCCA774C70C113DA372BC977C1`

- empty PostgreSQL 18 database: 0001-0011 applied;
- second migration runner execution: no-op;
- explicit 0001-0010 upgrade with an existing M5 Installation: 0011 PASS;
- observed backfill: `broker:NULL:1.0.0` (kind, client version, broker version);
- second application of SQL 0011: idempotent PASS;
- existing FORCE RLS, roles and grants unchanged.

## Gate executed so far

- Release build: PASS, zero warnings;
- ordinary .NET tests: 161 PASS, 10 conditional PostgreSQL tests qualified separately;
- PostgreSQL 18: 10/10 PASS;
- frontend lint/build: PASS;
- Vitest: 28/28 PASS;
- isolated browser mock: 37/37 PASS, including the Direct path;
- OpenAPI and runtime contract regenerated.
- `FULLSTACK-01`: PASS with Gateway container, PostgreSQL, enrollment, Published runtime and cleanup;
- secret scan, NuGet/npm vulnerability scan, SBOM and document validation: PASS;
- Docker cleanup: zero project containers, volumes and networks.

GitHub CI on the final HEAD and independent review remain. M5.5 is not yet Done and the PR
must not be merged automatically.
